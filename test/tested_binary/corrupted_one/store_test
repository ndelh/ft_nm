#include "corruptor.h":

void	oob_shoff(Elf64_Ehdr *head)
{
	head->e_shoff = UINT64_MAX;
}

void	overlapping_shoff(Elf64_Ehdr *head)
{
	head->e_shoff = 12;
}

void	oob_shstrndx(Elf64_Ehdr *head)
{
	head->e_shstrndx = 65000; 
}

void	unexisting_version(Elf64_Ehdr *head)
{
	head->e_ident[EI_VERSION] = 0;
}